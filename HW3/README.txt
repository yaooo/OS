# ECE434_Spring2017
To compile the code:
*make: make the executables
*make clean: remove the executables


Problem 4:

Mutex and condition variable:
****Note**** The code is designed to run for 30 seconds with 5 philosophers, as the default setting. You should be able to directly changed the "#define N" and  "#define runTime" from the code in order to modify the parameters.
	
To run the code:
	./philosopher_mutex_cond


Monitor:
****Note**** Program runs indefinitely, use Ctrl-C or Ctrl-Z (depend on your unix machine) to interrupt and stop when desired.

To run the code:
	./philosopher_monitor 

